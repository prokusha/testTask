#pragma once

namespace Window_Settings {

static int width = 800;
static int height = 800;

} // namespace Window_Settings

namespace Requests {

enum primitives {
    TRIANGLE,
    SQUARE,
    RECTANGLE,
    CIRCLE
};

} // namespace Requests
